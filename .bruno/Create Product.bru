meta {
  name: Create Product
  type: http
  seq: 4
}

post {
  url: http://localhost:8000/api/auth/token/
  body: json
  auth: inherit
}

body:json {
  { "username": "ad@ami.com", "password": "password"}
}

settings {
  encodeUrl: true
  timeout: 0
}
